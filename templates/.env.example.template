# Firebase Configuration
# Obtain these values from Firebase Console > Project Settings > General > Your apps
# Never commit the actual .env or .env.local files - they contain sensitive credentials

# Firebase API Key (public, but restricted by domain in Firebase Console)
NEXT_PUBLIC_FIREBASE_API_KEY="AIza****************************************xyz"

# Firebase Auth Domain (format: project-id.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"

# Firebase Project ID (unique identifier for your Firebase project)
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"

# Firebase Storage Bucket (format: project-id.appspot.com)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"

# Firebase Messaging Sender ID (numeric identifier)
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="123456789012"

# Firebase App ID (format: 1:number:platform:hash)
NEXT_PUBLIC_FIREBASE_APP_ID="1:123456789012:web:abc********123"

# Firebase Measurement ID (Google Analytics, optional)
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-**********"

# Server-side Firebase Admin SDK (optional, for backend operations)
# Download from Firebase Console > Project Settings > Service Accounts
# FIREBASE_ADMIN_SDK_JSON="path/to/serviceAccountKey.json"

# Application Environment
NODE_ENV="development"

# API Base URL (for external integrations if needed)
# NEXT_PUBLIC_API_BASE_URL="https://api.yourservice.com"
